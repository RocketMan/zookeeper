{% autoescape 'xml' %}
{% set feed = app.request.feed ?: 'No feed specified' %}
<channel>
  <title>Invalid feed: {{ feed }}</title>
  <link>{{ app.baseUrl }}</link>
  <description>Invalid feed: {{ feed }}</description>
</channel>
{% endautoescape %}
